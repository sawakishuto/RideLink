version: 2.1

orbs:
  ruby: circleci/ruby@1.2.0

jobs:
  build-and-test:
    macos:
      xcode: "15.4.0"  # このXcodeバージョンに合ったSwiftが含まれます
    environment:
      FL_OUTPUT_DIR: output
      FASTLANE_LANE: build_and_test
    shell: /bin/bash --login -o pipefail
    steps:
      - checkout
      - run:
          name: Fastlane
          command: bundle exec fastlane $FASTLANE_LANE
      - store_artifacts:
          path: output
      - store_test_results:
          path: output/scan

workflows:
  version: 2
  build:
    jobs:
      - build-and-test
